// Button styles enum
export enum ButtonStyles {
  PRIMARY = 'primary',    // Main button, used for primary actions
  SECONDARY = 'secondary', // Button for secondary actions
  ACTION = 'action',      // Button used for actions like submitting forms or triggering events
  LINK = 'link',          // Button styled as a link
  TOOLTIP = 'tooltip'     // Button that shows a tooltip when hovered over
}

// Button sizes enum
export enum ButtonSizes {
  SMALL = 'small',    // Small size button
  MEDIUM = 'medium',  // Medium size button
  LARGE = 'large'    // Large size button
}

